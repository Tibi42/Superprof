#include <stdio.h>

    int somme(int tableau[])
    {
       
        int somme = 0;
        for (int i = 0; i <= 9; i++)
        {
            somme += tableau[i];
        }
        return somme;
    }

int main ()
{
   
    int myNumbers[] = {1, 50, 75, 100, 5, 1, 75, 3, 100, 1};
    //scanf ("%d", & n );
    printf ("%d\n", somme(myNumbers) );

}